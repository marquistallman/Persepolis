📋 Lista de Tareas Pendientes (To-Do)

1. Prioridad ALTA (Frontend y Experiencia de Usuario)
   * Ventana Principal (Home):
     - Arreglar la disposición visual de la ventana principal.
     - Implementar la sección de "Populares" en el inicio (actualmente carga una búsqueda por defecto o nada).

2. Prioridad MEDIA (Completar Scrapers)
   * Extracción de Tags (Faltantes):
     - Wallhaven (en lista de búsqueda), WallpaperFlare, Peakpx, MotionBackgrounds, WallpaperAbyss, WallpaperWaves.
   * Validación de Datos:
     - Asegurar que no se guarden wallpapers con imágenes rotas o URLs vacías.

3. Prioridad MEDIA (Estabilidad y Anti-Bloqueo)
   * Mejorar Anti-Scraping:
     - Implementar "Backoff" (esperas aleatorias entre peticiones) para complementar la rotación de User-Agents.
   * Manejo de Errores HTTP:
     - Eliminar .ignoreHttpErrors(true) de SitioBase y manejar códigos 404/500 correctamente.